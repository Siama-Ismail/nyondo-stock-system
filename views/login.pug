doctype html
html
  head
    meta(charset='UTF-8')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title Login - NYONDOStock
    link(href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css' rel='stylesheet')
    link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css')
    style.
      body {
        background-color: #E5E7EB !important; /* Changed to Silver Gray */
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0;
        font-family: sans-serif;
      }
      .login-card {
        background: #111827 !important; /* Changed to Matte Black */
        border-radius: 12px;
        padding: 40px;
        width: 100%;
        max-width: 400px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.3);
      }
      .brand-title {
        font-weight: 800;
        color: #E5E7EB; /* Changed to Silver Gray */
        text-align: center;
        margin-bottom: 5px;
      }
      .brand-title span {
        color: #10B981; /* Changed to Emerald Green */
      }
      .form-label {
        color: #E5E7EB; /* Changed to Silver Gray for contrast on dark background */
        font-weight: 600;
        font-size: 0.9rem;
      }
      .form-control {
        border: 2px solid #E5E7EB; /* Changed to Silver Gray */
        padding: 12px;
      }
      .form-control:focus {
        border-color: #10B981; /* Changed to Emerald Green */
        box-shadow: none;
      }
      .btn-login {
        background-color: #10B981 !important; /* Changed to Emerald Green */
        border: none !important;
        color: white !important;
        font-weight: 700;
        padding: 14px;
        width: 100%;
        margin-top: 10px;
        transition: 0.2s;
      }
      .btn-login:hover {
        background-color: #059669 !important; /* Darker Emerald Green for hover */
      }
      .footer-text {
        text-align: center;
        margin-top: 20px;
        color: #9CA3AF; /* Adjusted gray for contrast */
        font-size: 0.8rem;
      }
  body
    .login-card
      h2.brand-title
        | NYONDO
        span STOCK
      p.text-center.text-muted.small.mb-4 Inventory Management System
      if error
        p.text-danger.text-center #{error}
      form(action='/login' method='post')
        .mb-3
          label.form-label Email Address
          input.form-control(type='email' name='email' placeholder='admin@nyondo.com' required)
        .mb-4
          label.form-label Password
          input.form-control(type='password' name='password' placeholder='••••••••' required)
        button.btn.btn-login(type='submit') SIGN IN
        .text-center.mt-3
          a.text-decoration-none.small(href='#' style='color: #10B981;') Forgot Password?
      .footer-text
        | © 2026 NYONDO General Hardware LTD